# Исходники листков (LaTeX)

Исходники новых листков. Структура папок повторяет `sheets/`. Готовые PDF лежат в `sheets/`.

Сборка: `pdflatex` (TeX Live, пакеты с кириллицей и cm-super), дважды. Дата в шапке — день сборки.
